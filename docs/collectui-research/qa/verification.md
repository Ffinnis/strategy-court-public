# Guide verification

Verified on 31 August 2026 using the in-app browser.

- 24 unique selected source URLs, 24 surveyed category pages, 62 application ideas, and 13 proposed motion specifications.
- All 24 primary reference screenshot files and all listed repository touchpoints exist.
- All 58 source screenshots remain unmodified browser captures. Additional guide QA images live in this directory and are not included in the source screenshot count.
- No unresolved template markers in the generated HTML.
- Workflow filter set to Variants returns one reference. Adding P0 retains that reference.
- An unmatched search returns zero results with a clear-filters action. Clearing returns 24 references.
- Both example evidence selections update the status, row, inspector, and chart interval. The reduced-motion checkbox changes its checked state.
- Mobile layout verified at 390 × 844. The document width remains 390 px. Wide documentation tables scroll within their own containers.
- Mobile selection controls update the example inspector. The temporary browser viewport override was reset.
- No browser script errors were reported during the guide checks.

This verifies the research artifact. It does not claim an accessibility certification, exhaustive performance testing, or a test of the authenticated Strategy Court workflows. The interactive sketch uses illustrative data and makes no network mutations.
